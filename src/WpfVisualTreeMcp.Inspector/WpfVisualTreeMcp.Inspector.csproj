<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net48;net10.0-windows</TargetFrameworks>
    <OutputType>Library</OutputType>
    <RootNamespace>WpfVisualTreeMcp.Inspector</RootNamespace>
    <AssemblyName>WpfVisualTreeMcp.Inspector</AssemblyName>
    <LangVersion>12.0</LangVersion>
    <Nullable>enable</Nullable>
    <UseWPF Condition="'$(TargetFramework)' == 'net10.0-windows'">true</UseWPF>
  </PropertyGroup>

  <!-- .NET Framework 4.8: explicit WPF references -->
  <!-- (net10.0-windows pulls these in automatically via UseWPF) -->
  <ItemGroup Condition="'$(TargetFramework)' == 'net48'">
    <Reference Include="PresentationCore" />
    <Reference Include="PresentationFramework" />
    <Reference Include="WindowsBase" />
    <Reference Include="System.Xaml" />
    <!-- UI Automation provider interfaces used by ControlInteractor (click support) -->
    <Reference Include="UIAutomationProvider" />
    <Reference Include="UIAutomationTypes" />
    <PackageReference Include="System.IO.Pipes" Version="4.3.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\WpfVisualTreeMcp.Shared\WpfVisualTreeMcp.Shared.csproj" />
  </ItemGroup>

</Project>
